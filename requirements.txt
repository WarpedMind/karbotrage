aiohttp>=3.8.0
pydantic>=2.0
websockets>=12.0
pyyaml>=6.0
python-json-logger>=2.0.0
structlog>=23.0.0
tenacity>=8.0.0
aiosqlite>=0.19.0
anthropic>=0.25.0
cryptography>=41.0.0
pytest>=7.0.0
pytest-asyncio>=0.20.0
black>=22.0.0
flake8>=4.0.0
python-dotenv>=1.0.0
# Used ONLY by the offline research packages (backtest/, canary/), never by the
# live trading path — which is async and uses aiohttp. Added Session 32: it had
# been an undeclared dependency since backtest/ shipped in Session 31, and it
# surfaced as a ModuleNotFoundError the first time anything imported those
# packages on the VPS. It was present locally by coincidence of the dev
# environment, which is exactly how an undeclared dependency stays invisible.
requests>=2.31.0
